<header class="page-head">
  <div class="wrap page-head__in">
    <div>
      <span class="eyebrow">Account · Reset</span>
      <h1 class="h1">Forgot your password?</h1>
      <p>Enter your email and we'll send you instructions to reset your password.</p>
    </div>
  </div>
</header>

<div class="wrap">
  <section class="auth card">
    <div class="card__body">
      <.form :let={f} for={%{}} as={:user} action={~p"/users/reset_password"} class="auth__form">
        <div class="field">
          {label(f, :email, "Email")}
          {email_input(f, :email, required: true, placeholder: "you@example.com")}
        </div>
        {submit("Send reset instructions", class: "btn btn--primary btn--block")}
      </.form>

      <div class="auth__links">
        <.link navigate={~p"/users/log_in"}>Back to sign in</.link>
        <.link navigate={~p"/users/register"}>Create an account</.link>
      </div>
    </div>
  </section>
</div>
