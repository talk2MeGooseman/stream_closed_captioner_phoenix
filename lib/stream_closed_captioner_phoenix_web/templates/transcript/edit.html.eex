<h1>Edit Transcript</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.transcript_path(@conn, :update, @transcript)) %>

<span><%= link "Back", to: Routes.transcript_path(@conn, :index) %></span>
