# Orchestration Log — Trinity Fixes Implementation Phase

**Timestamp:** 2026-04-28T04:49:40Z  
**Agent:** Trinity (Backend Implementation)  
**Mission:** Coordinate code changes for P1 (dead bot config) + P2 (Dockerfile casing)

---

## Scope & Charter

Trinity chartered to:
1. Remove dead `:bot` config block from config/runtime.exs (Decision 2026-04-25 TMI removal)
2. Fix Dockerfile casing: line 17 `as` → `AS`
3. Verify local compile + run subset of tests
4. Hand off to Tank for full CI verification

---

## Implementation Plan

### Change 1: Kill Dead Bot Config (config/runtime.exs)
- **Lines 117-123:** Remove `:bot` configuration block
- **Lines 54-60:** Keep existing `:live_reload` (not bot-related)
- **Rationale:** TMI supervisor removed (Decision 2026-04-25). Config block is dead code, confuses operators.
- **Verify:** `mix compile --warnings-as-errors`

### Change 2: Dockerfile Casing Fix (Line 17)
- **Line 17:** Change `FROM ${BUILDER_IMAGE} as builder` → `FROM ${BUILDER_IMAGE} AS builder`
- **Rationale:** Lint convention (uppercase keywords). Removes CI lint noise.
- **Verify:** `docker build --dry-run` or wait for CI

### Change 3: Documentation Update (docs/coolify-migration.md)
- **Scope:** Remove TWITCH_CHAT_OAUTH from required env vars list
- **Rationale:** Var no longer used (TMI removed). Prevents operator confusion.
- **Owner:** Scribe (as part of decision merge), but Trinity may batch this

---

## Execution Steps

1. **Edit config/runtime.exs:** Remove lines 117-123 (`:bot` block)
2. **Edit Dockerfile:** Line 17 change `as` → `AS`
3. **Run `mix compile`:** Verify no errors/warnings
4. **Run `mix test --include integration`:** Spot-check critical paths
5. **Commit:** Brief message linking to P1/P2 decisions
6. **Hand off to Tank:** Full test suite + CI verification

---

## Status

⏳ **PENDING** — Implementation staged. Awaiting execution trigger from Orchestration (Morpheus).

---

## Related Decisions

- **Decision 2026-04-25:** Remove Dead TMI/TwitchBot Code Path (code already deleted; this is config cleanup)
- **Decision 2026-04-26:** Coolify Deployment Infra Scoping (identifies Dockerfile casing)
- **Decision (Proposed):** Deploy Remediation Plan (P1 = this work, P2 = this work)
