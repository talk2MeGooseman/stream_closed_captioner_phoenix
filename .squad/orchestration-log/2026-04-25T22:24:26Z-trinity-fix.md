# Orchestration Log — trinity-fix

**Timestamp:** 2026-04-25T22:24:26Z  
**Agent:** Trinity (Backend Dev)  
**Task:** Fix get_user/3 unreachable nil branch in resolvers/accounts.ex

## Work Summary

Identified and fixed unreachable code in `lib/stream_closed_captioner_phoenix_web/resolvers/accounts.ex:33–39`.

**Issue:** The `get_user/3` resolver called `Accounts.get_user!(id)` (bang variant, raises `Ecto.NoResultsError`) but then used `case` to pattern-match `nil`. Bang functions never return `nil`, making the nil branch dead code.

**Fix:** Replaced `case`-on-nil with `rescue Ecto.NoResultsError` pattern, maintaining error semantics.

## Files Modified

1. `lib/stream_closed_captioner_phoenix_web/resolvers/accounts.ex` — removed dead code, added rescue clause

## Validation

- All 10 tests pass
- Compile clean
- No warnings

## Handoff Notes

- Minimal surgical change — only modified the broken function
- Preserves error message contract
- Follows Elixir convention: bang functions raise, rescue handles exceptions
- No need to add non-bang variant to Accounts context for single use case
